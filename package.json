{
  "name": "higgins-challenge",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:archive": "ch5-cli archive -p higgins-challenge -d dist -o tsw",
    "buildall": "npm run build && npm run build:archive",
    "upload": "ch5-cli deploy -H 192.168.1.188 -t touchscreen tsw/higgins-challenge.ch5z",
    "dump": "npm run build && npm run build:archive && npm run upload"
  },
  "devDependencies": {
    "@crestron/ch5-utilities-cli": "^2.0.0",
    "vite": "^7.0.4"
  },
  "dependencies": {
    "@crestron/ch5-crcomlib": "^2.15.0",
    "vite-plugin-html-inject": "^1.1.2"
  }
}
